defmodule Enigma.Mixfile do
  use Mix.Project

  def project do
    [
      app: :enigma,
      version: "0.1.0",
      elixir: "~> 1.3",
      deps: deps()
    ]
  end

  defp deps do
    []
  end
end
